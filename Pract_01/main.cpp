#include "src/header.hpp"
#include <iostream>
#include <vector>

int main()
{
    std::vector<Account> accounts {};
    std::cout << "Add account: " << std::endl;
    while(true)
    {
        return 0;
    }
    test_Func();
    system("pause");
    return 0;
}