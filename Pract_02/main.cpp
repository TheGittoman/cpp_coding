#include "src/llist.hpp"
#include "src/leetcode.hpp"
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <algorithm>
#include <vector>
#include <climits>

std::vector<int> replaceElements(std::vector<int> &arr)
{

}

int main()
{
	std::vector<int> nums{0, 2, 3, 4, 5, 2, 1, 0};
	std::vector<int> numsReplaced{replaceElements(nums)};
	std::cout << std::endl;
	system("pause");
	return 0;
}